# Managed by gentoo-build. Keyed on PARTUUID (INVARIANT: never /dev/vdaX).
PARTUUID={{ esp_partuuid }}   /boot/efi  vfat        defaults,noatime  0 2
PARTUUID={{ root_partuuid }}  /          {{ root_fs }}  defaults,noatime  0 1
